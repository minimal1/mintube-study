doctype html
html
    head
        script(src="https://kit.fontawesome.com/0ac70a1dad.js",crossorigin="anonymous")
        title #{pageTitle} | #{siteTitle}
    body

        include ../partials/header
        main
            block content

        include ../partials/footer
        
